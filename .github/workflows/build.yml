name: Build OsirisCLI (Windows)

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build-windows:
    name: Build Windows Setup (OsirisSetup.exe)
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set version (from tag or fallback)
        shell: pwsh
        run: |
          $ver = "v1.2.3"
          if ("${{ github.ref_type }}" -eq "tag") {
            $ver = "${{ github.ref_name }}"
          }
          Write-Host "Using version: $ver"
          "OSIRIS_VERSION=$ver" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies + PyInstaller
        shell: pwsh
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller

      - name: Build OsirisCLI (ONEDIR, console, icon embedded)
        shell: pwsh
        run: |
          pyinstaller --noconfirm --clean `
            --name "OsirisCLI" `
            --onedir `
            --console `
            --icon "assets/icon.ico" `
            --collect-all rich `
            --collect-all textual `
            --collect-submodules rich._unicode_data `
            app/main.py

      - name: Install Inno Setup
        shell: pwsh
        run: |
          choco install innosetup -y

      - name: Ensure installer_output folder exists
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path installer_output | Out-Null

      - name: Compile Windows Installer
        shell: pwsh
        run: |
          iscc installer/osiris_setup.iss

      - name: Upload Windows Installer
        uses: actions/upload-artifact@v4
        with:
          name: OsirisSetup-Windows-${{ env.OSIRIS_VERSION }}
          path: installer_output/*.exe
